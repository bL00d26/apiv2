import configure from "@vendia/serverless-express/src/configure"

export default configure;
export { default as configure } from "@vendia/serverless-express/src/configure"
export { getCurrentInvoke } from "@vendia/serverless-express/src/current-invoke"
